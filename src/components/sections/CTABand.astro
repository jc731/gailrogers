---
interface Props {
  heading: string;
  text?: string;
  primaryCTA?: {
    text: string;
    link: string;
  };
  secondaryCTA?: {
    text: string;
    link: string;
  };
  className?: string;
}

const { heading, text, primaryCTA, secondaryCTA, className = '' } = Astro.props;
---

<section class={`bg-brand-primary text-white py-12 px-4 ${className}`}>
  <div class="container mx-auto max-w-4xl text-center">
    <h2 class="text-3xl font-bold mb-4">{heading}</h2>
    {text && (
      <p class="text-xl mb-8">{text}</p>
    )}
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      {primaryCTA && (
        <a 
          href={primaryCTA.link}
          class="inline-block bg-white text-brand-primary px-6 py-3 rounded-lg font-semibold hover:bg-brand-light transition-colors"
        >
          {primaryCTA.text}
        </a>
      )}
      {secondaryCTA && (
        <a 
          href={secondaryCTA.link}
          class="inline-block bg-transparent border-2 border-white text-white px-6 py-3 rounded-lg font-semibold hover:bg-white/10 transition-colors"
        >
          {secondaryCTA.text}
        </a>
      )}
    </div>
  </div>
</section>

