---
interface Props {
  id: string;
  name: string;
  label: string;
  type?: 'text' | 'email' | 'tel' | 'url' | 'number';
  required?: boolean;
  placeholder?: string;
  value?: string;
  error?: string;
  className?: string;
}

const {
  id,
  name,
  label,
  type = 'text',
  required = false,
  placeholder,
  value,
  error,
  className = '',
} = Astro.props;
---

<div class={`form-field ${className}`}>
  <label for={id} class="block text-sm font-medium text-gray-900 mb-1">
    {label}
    {required && <span class="text-red-600" aria-label="required">*</span>}
  </label>
  <input
    type={type}
    id={id}
    name={name}
    value={value}
    placeholder={placeholder}
    required={required}
    aria-required={required}
    aria-invalid={error ? 'true' : 'false'}
    aria-describedby={error ? `${id}-error` : undefined}
    class={`w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-heading-subheading focus:border-heading-subheading transition-colors ${
      error ? 'border-red-500' : ''
    }`}
  />
  {error && (
    <p id={`${id}-error`} class="mt-1 text-sm text-red-600" role="alert">
      {error}
    </p>
  )}
</div>

